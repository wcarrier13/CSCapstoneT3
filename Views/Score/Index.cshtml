@model IEnumerable<Lizst.Models.Score>

@{
    ViewData["Title"] = "Index";
}

<h1 style="padding-bottom:10px; padding-top:10px;">Scores</h1>
<p>
    <a class="btn btn-info" asp-action="AddScore">Add Score</a>
</p>
<table class="table table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ScoreId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Composer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Genre)
            </th>

            <th></th>

        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ScoreId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Composer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Genre)
            </td>

            <td>
                <a style="color:#17a2b8;" asp-action="EditScore" asp-route-id="@item.ScoreId">Edit</a> |
                <a style="color:#17a2b8;" asp-action="Details" asp-route-id="@item.ScoreId">Details</a> |
                <a style="color:#17a2b8;" asp-action="Delete" asp-route-id="@item.ScoreId">Delete</a> |
                <a style="color:#17a2b8;" asp-controller="Cart" asp-action="AddToCart" asp-route-id="@item.ScoreId">Add to Cart</a>
            </td>
        </tr>
}
    </tbody>
</table>
